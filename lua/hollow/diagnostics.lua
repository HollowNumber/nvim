vim.diagnostic.config({
    virtual_text = true,    -- show inline
    signs = true,           -- show icons in the gutter
    underline = true,       -- underline errors/warnings
    update_in_insert = false, -- optional: update while typing
})
